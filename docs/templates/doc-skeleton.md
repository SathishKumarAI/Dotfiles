<!--
Doc skeleton — copy into a repo's docs/ and fill in. Delete sections you don't
need. Keep it scannable: lead with the point, prefer tables and runnable
examples, and explain the *why* behind non-obvious choices.
-->
# <Title — what this document is>

> One-sentence summary: what this covers and who it's for.

## Context / Why

Why this exists — the problem it solves or the decision it records. What prompted
it, and the intended outcome.

## Overview

The 2–3 things a reader must understand first. A small diagram or table if it
helps.

## Usage / How-to

Concrete, runnable steps. Prefer copy-pasteable commands:

```bash
<command>
```

| Command / Option | What it does |
|------------------|--------------|
| `...` | ... |

## How it works (optional)

The mechanism, for readers who need to modify it. Link to the canonical code
paths (`path/to/file.ext`) rather than restating them.

## Gotchas / Troubleshooting

Known failure modes and their fixes. Symptom → cause → fix.

## See also

- Links to related docs / code (one source of truth — link, don't duplicate).
